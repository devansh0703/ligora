# Core dependencies
numpy>=1.24.0
requests>=2.28.0

# Cheminformatics (required for fingerprints, similarity, SDF, descriptors)
rdkit-pypi>=2026.03.0

# Optional engine adapters (bundled separately or installed in workspace)
# - AutoDock Vina (Apache 2.0)
# - gnina (GPL/Apache)
# - OpenMM (open, Python)
# - PySCF (Apache 2.0)
# - PLIP (GPL) for contact analysis

# Snapcraft parts reference:
# - python-requirements can point at this file
# - See snap/snapcraft.yaml for installed system packages
