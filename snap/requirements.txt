# Core dependencies (mirrors backend/requirements.txt)
numpy>=1.24.0
requests>=2.28.0

# Cheminformatics (fingerprints, similarity, SDF, bond perception, descriptors)
rdkit>=2024.9.1

# External tools bundled via snap parts or system packages (never faked):
# - PLIP (GPL) for contact analysis        -> plip part / apt install plip
# - Open Babel for PDBQT prep/minimization -> apt install obabel
# - AutoDock Vina (Apache 2.0) for docking -> vina part (built from source)
